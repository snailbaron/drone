#pragma once

#include <string>

using namespace std::literals::string_literals;

namespace conf {

const auto GameFps = 60;

const auto WindowTitle = "Drone"s;
const auto WindowWidth = 1024;
const auto WindowHeight = 768;

} // namespace conf